# Airflow and workflow orchestration
apache-airflow
apache-airflow-providers-celery
apache-airflow-providers-fab

# Database
psycopg2-binary
sqlalchemy>=2.0,<3.0

# Time utilities
pendulum
pytz

# Data analysis
pandas
pydantic

# HTTP requests
requests

# CLI tools
fire

# Garmin data processing
# garminconnect is replaced by the vendored garmin_client/ module under
# dags/pipelines/garmin/. The two transitive deps it brought in (curl-cffi for
# TLS impersonation, ua-generator for browser User-Agent rotation) are now
# explicit because the vendored auth strategies depend on them directly.
curl-cffi
fitdecode
ua-generator