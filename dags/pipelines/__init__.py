"""
Data pipeline implementations.

This package contains individual DAG directories, each with their own:
- DAG definition (dag.py)
- Task implementations (tasks/ subdirectory)
- SQL definitions (sql/ subdirectory)
- Pipeline-specific configuration (config/ subdirectory)
- Documentation (README.md)

Each pipeline follows a consistent structure for maintainability and reusability.
"""
