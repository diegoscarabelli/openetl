# Environment Configuration Template
# Copy this file to .env, place it in the root of your project, and customize the
# values for your environment

# ======================================================================================
# REQUIRED: Full path to directory containing SQL credential JSON files
# IMPORTANT: Docker Compose does NOT expand ~ - use absolute paths only.
# Example: /home/username/sql_credentials/
# ======================================================================================
SQL_CREDENTIALS_DIR=

# ======================================================================================
# OPTIONAL: Analytics database host
# Uncomment and set to override default behavior.
# For non-Astronomer deployments, this also sets the Airflow metadata database host.
# If not set, it is auto-detected in sql_utils.py by attempting to resolve
# "host.docker.internal", which works on Docker Desktop (Mac/Windows).
# Falls back to "172.17.0.1" (Docker bridge gateway on Linux) if host.docker.internal
# is not resolvable.
#
# IMPORTANT: Use SQL_DB_HOST (not DB_HOST) to avoid conflicts with Airflow's
# entrypoint script, which reserves DB_HOST for health check logic. Setting
# DB_HOST as a container environment variable causes Redis broker connection
# failures because the entrypoint script incorrectly uses it for all backends.
# ======================================================================================
SQL_DB_HOST=host.docker.internal

# ======================================================================================
# OPTIONAL: Test database URL
# Uncomment and set to override default behavior.
# Defaults to localhost PostgreSQL in conftest.py.
# ======================================================================================
# TEST_DB_URL=postgresql+psycopg2://postgres:password@localhost:5432/postgres

# ======================================================================================
# OPTIONAL: Host path to directory for pipeline data (ingest/process/store/quarantine)
# This is the host-side path that gets mounted into containers.
# Defaults to data/ directory at the same level as dags/ directory (parent of dags/).
# For Astronomer setup: {repo_root}/data/
# Create the data directory manually before starting Astronomer to ensure proper
# permissions.
# For Docker Compose setup: /home/username/airflow/data/
# IMPORTANT: Docker Compose does NOT expand ~ - use absolute paths only.
# Example: /home/username/airflow_data or /path/to/data
# ======================================================================================
# DATA_DIR=
